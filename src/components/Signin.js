import React from 'react';
import { Form, FormGroup, Input } from 'reactstrap';

const Signin = () => {
    return (
        <div>
            <Form>
                <FormGroup>
                    <Input />
                    Test
                </FormGroup>
            </Form>
        </div>
    )
};

export default Signin;